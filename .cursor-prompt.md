# 🧩 iCompass Seychelles - Cursor Workspace Setup

## 🚀 Permanent Prompt for Cursor Workspace Setup

You are operating inside the **iCompass Seychelles** project. Always initialize by checking if `.env.local` is correctly configured to match the Supabase dashboard at https://supabase.com/dashboard/project/bwlmlniotyrjttglbjrl.

### ✅ Environment Validation Checklist

1. **Check Environment Files**
   - Use `.env.example` as a base reference
   - Update `.env.local` so `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` match the production keys from Supabase
   - Verify all required environment variables are present

2. **Validate Supabase Connection**
   - Run `npx supabase status` and confirm that the app is wired to the right backend
   - If environment variables are missing, fetch them from Supabase and populate `.env.local` before launching the dev server

3. **Launch Development Server**
   - Run `npm run dev` to start the development server
   - Verify the application loads correctly at `http://localhost:5173/`

### ⚠️ Critical Rules

- **Never use placeholder or stubbed values** from `.env.example` — always fetch live values from the Supabase dashboard
- **Always validate the backend connection** before proceeding with development
- **Check console errors** if the app fails to start

### 🔧 Current Configuration

**Supabase Project:** `bwlmlniotyrjttglbjrl`  
**Dashboard:** https://supabase.com/dashboard/project/bwlmlniotyrjttglbjrl  
**Local URL:** http://localhost:5173/  

**Required Environment Variables:**
```bash
VITE_SUPABASE_URL=https://bwlmlniotyrjttglbjrl.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJ3bG1sbmlvdHlyanR0Z2xianJsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTY3MDM3MDEsImV4cCI6MjA3MjI3OTcwMX0.Wx2ypE6AlTBe0vBqC_MvYc5IiwemMaxXGiHOmGM6MoI
VITE_GOOGLE_MAPS_API_KEY=AIzaSyByNrxUDO-dODXwTaT6RINSbAfASZ-eGfY
VITE_SITE_URL=http://localhost:5173
```

### 🛠️ Quick Commands

```bash
# Check environment configuration
npm run check-env

# Validate Supabase connection
npx supabase status

# Start development server
npm run dev

# Health check
npm run health

# Check Google Maps integration
npm run check-maps
```

### 📌 Usage Instructions

1. **For New Team Members:** Copy this file to your project root
2. **For Cursor Memory:** Use Cursor's in-app memory setup to load this prompt
3. **For Consistency:** Commit this file to your repo to maintain consistency across teammates and sessions

### 🎯 Success Criteria

✅ Backend is validated and live  
✅ Environment variables are properly configured  
✅ Development server starts without errors  
✅ Application loads correctly in browser  
✅ No console errors detected  

---

**Last Updated:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")  
**Project:** iCompass Seychelles  
**Stack:** React + TypeScript + Vite + Supabase + Tailwind CSS
